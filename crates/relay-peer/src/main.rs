//! `relay-peer` — stateless SSH proxy for the VPS.
//!
//! Skeleton entry point. Accepts the reverse-SSH connection from `relay` and
//! forwards inbound public traffic through it. Implemented in Phase 4.

fn main() {
    println!("relay-peer {} — not yet implemented", env!("CARGO_PKG_VERSION"));
}
