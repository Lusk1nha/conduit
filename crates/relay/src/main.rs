//! `relay` — standalone reverse-SSH webhook tunnel daemon.
//!
//! Skeleton entry point. The Axum HTTP server, delivery worker pool, SSH
//! tunnel task, and SQLite event log are implemented in Phases 3 and 4.

fn main() {
    println!("relay {} — not yet implemented", env!("CARGO_PKG_VERSION"));
}
